# ============================================================
# SevaSetu AI — Python Backend Dependencies
# Author: Rahul Jha | Made in India 🇮🇳
# ============================================================

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # File uploads
httpx==0.27.0                  # Async HTTP client

# ── Database ──────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0                # Async PostgreSQL driver
alembic==1.13.1                # DB migrations
redis==5.0.4                   # Redis client

# ── Authentication ────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # Password hashing
pydantic[email]==2.7.1
pydantic-settings==2.3.0

# ── AI / RAG ─────────────────────────────────────────────────
google-genai>=1.30.0,<2.0.0  # Gemini API (current SDK)
openai==1.30.1                 # OpenAI fallback
chromadb==0.5.0                # Vector database
sentence-transformers==3.0.0   # Embeddings
langdetect==1.0.9              # Language detection

# ── OCR ───────────────────────────────────────────────────────
pytesseract==0.3.10            # Tesseract Python binding
Pillow==10.4.0                 # Image processing
PyMuPDF==1.24.11                # PDF processing (fitz)
opencv-python-headless==4.9.0.80
numpy==1.26.4

# ── PDF Report Generation ─────────────────────────────────────
reportlab==4.2.0               # PDF generation

# ── Email ────────────────────────────────────────────────────
aiosmtplib==3.0.1              # Async SMTP
jinja2==3.1.4                  # Email templates

# ── Translation ───────────────────────────────────────────────
# Translation handled by Gemini API (googletrans intentionally excluded
# because it pins httpx==0.13.3, conflicting with FastAPI/OpenAI)

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
aiofiles==23.2.1               # Async file I/O
ujson==5.10.0                  # Fast JSON
loguru==0.7.2                  # Better logging
python-slugify==8.0.4

# ── Monitoring ────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
factory-boy==3.3.0
